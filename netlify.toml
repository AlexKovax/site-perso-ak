[build]
  command = "npm run build"
  publish = "dist"

# Remove trailing slashes (except root)
[[redirects]]
  from = "/*/"
  to = "/:splat"
  status = 301

# SPA fallback
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
